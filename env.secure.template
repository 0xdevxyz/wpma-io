# ============================================
# WPMA.io - Sichere Environment-Konfiguration
# ============================================
# 
# WICHTIG: Diese Datei enthält Beispielwerte.
# Kopiere diese Datei als .env und ersetze alle Werte!
# 
# NIEMALS diese Datei mit echten Werten in Git committen!
# ============================================

# === Server Konfiguration ===
NODE_ENV=production
PORT=8000

# === Datenbank (PostgreSQL) ===
# Generiere sicheres Passwort mit: openssl rand -base64 32
DATABASE_URL=postgresql://wpma_user:CHANGE_ME_STRONG_PASSWORD@wpma-postgres:5432/wpma_db
DATABASE_SSL=false
DATABASE_POOL_MAX=20
DATABASE_IDLE_TIMEOUT=30000
DATABASE_CONNECT_TIMEOUT=5000

# Für docker-compose Postgres Container
POSTGRES_DB=wpma_db
POSTGRES_USER=wpma_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# === Redis ===
# Generiere sicheres Passwort mit: openssl rand -base64 32
REDIS_HOST=wpma-redis
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD
REDIS_DB=3

# === JWT Authentifizierung ===
# Generiere Secret mit: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_64
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# === OpenAI / LLM ===
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENROUTER_API_KEY=sk-or-your-openrouter-key-here

# === AWS S3 für Backups ===
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=eu-central-1
AWS_BACKUP_BUCKET=wpma-backups

# === iDrive E2 (Alternative zu S3) ===
IDRIVE_E2_ACCESS_KEY_ID=your-idrive-access-key
IDRIVE_E2_SECRET_ACCESS_KEY=your-idrive-secret-key
IDRIVE_E2_BACKUP_BUCKET=wpma-backups-idrive

# === Stripe Payments ===
STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# === E-Mail (SMTP) ===
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@wpma.io
SMTP_PASS=CHANGE_ME_SMTP_PASSWORD
SMTP_FROM_NAME=WPMA.io
SMTP_FROM_EMAIL=noreply@wpma.io

# === Sentry Error Tracking ===
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# === Frontend URLs ===
FRONTEND_URL=https://app.wpma.io
LANDING_URL=https://wpma.io

# === Reverse Proxy / Domains ===
VIRTUAL_HOST=api.wpma.io
LETSENCRYPT_HOST=api.wpma.io
VIRTUAL_PORT=8000

# ============================================
# Sicherheits-Checkliste:
# ============================================
# [ ] Alle CHANGE_ME Werte ersetzt?
# [ ] Passwörter sind 32+ Zeichen lang?
# [ ] .env ist in .gitignore?
# [ ] Backup der .env an sicherem Ort?
# ============================================

